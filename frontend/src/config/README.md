# config/

Application constants — no logic, no components.

- `constants.ts` — Tag colors for file extensions, admin tab definitions, labels, and descriptions.

**Rules**: Only `const` exports. No functions, no React, no imports from other app modules.
