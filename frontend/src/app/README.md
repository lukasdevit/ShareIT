# app/

Route pages only — no business logic, no API calls.

- **Must**: Page components, layouts, route groups.
- **Must not**: Business logic, API calls, complex state.
- Import components from `@/components/`, hooks from `@/hooks/`, providers from `@/features/`.
