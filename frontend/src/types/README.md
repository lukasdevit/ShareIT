# types/

Global TypeScript interfaces used across the app.

- `FileInfo`, `FilePage` — File listing responses.
- `StorageInfo` — Storage usage data.
- `UserInfo`, `AdminUserRow` — User/auth types.

**Rules**: Only interfaces/types. No runtime code, no React.
